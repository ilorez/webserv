[ ] add isCGI method to request call, that find a pattern in path like:
    - /upload/script.sh // store the script in /cgi
    - /cgi/script.sh run script (get->wihtout input to fd) (post->with input)
[ ] after know if it is a cgi store it, this is alaoui work
[ ] if it was cgi but its need to run script, this where comes the server part
[ ] look to tldraw

